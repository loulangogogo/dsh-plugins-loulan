# dsh.env —— 项目临时环境变量（各脚本启动时 source，见 package.json 的 dev / link:dsh / test-mcp）。
#
# DeepSeek Harness checkout 目录。
# 优先级：外部已设置的 DSH_HARNESS 环境变量 > 本文件默认值。
# 若你的 harness 不在默认位置，改这里的默认值即可；也可在调用前 export DSH_HARNESS 覆盖。
DSH_HARNESS="${DSH_HARNESS:-$HOME/.dsh/deepseek-harness}"
export DSH_HARNESS
