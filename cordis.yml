# 多插件开发项目的本地覆盖层（patch overlay）。
#
# 注意：`name` 必须是绝对路径 —— loader 会用 profile 目录（而非本文件所在目录）
# 去解析模块。若项目被移动到其它位置，请同步更新下面两处绝对路径。

- insert:
    - id: hello
      name: ./packages/hello/src/index.ts

    - id: dsh-loulan-mcp
      name: ./packages/mcp/src/index.ts

